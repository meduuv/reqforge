[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="reqforge"
version="0.1.0"
description="HTTP request builder for API diagnostics"
requires-python=">=3.10"
[project.scripts]
reqforge="reqforge.cli:main"
[tool.setuptools.packages.find]
where=["src"]
